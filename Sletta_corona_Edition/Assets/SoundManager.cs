using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SoundManager : MonoBehaviour
{
    /*
    public static SoundManager snd;

    private AudioSourse audioSrc;

    private AudioClip[] attacSounds;
    
    private int randomSounds;


    // Start is called before the first frame update
    void Start()
    {
        snd = this;
        audioSrc = GetComponent<AudioSource>();
        attacSounds = Sounds.LoadAll<AudioClip>("attac");
    }

    public void PlayAttacSounds()
    {
        randomSounds = Random.Range(0, 4);
        audioSrc.PlayOneShot(attacSounds[randomSounds]);
    }
    */
}
